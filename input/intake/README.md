# Unclassified Incoming Files

Place new or uncertain files here until the maintainer classifies their access,
Git and sharing rules. The folder is ignored except for this README. Assistants
must not enumerate, preview or read its contents by default.
